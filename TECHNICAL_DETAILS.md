# Technical Deep Dive: 開発の詳細と意思決定プロセス

本ドキュメントでは、ポートフォリオ開発における技術的な選定理由、設計思想、および直面した課題の解決プロセスについて詳細に記述します。

---

## 1. システムアーキテクチャ
本システムは、Javaによるデータ生成、DjangoによるWeb制御、Rによる高度な統計解析を組み合わせた、異種言語統合型のパイプラインを採用しています。

- **Data Generation**: Java (GSON)
- **Web Framework**: Django (Python)
- **Statistical Analytics**: R (ggplot2, stats)
- **Infrastructure**: Docker / AWS Fargate

---

## 2. 統計的アプローチとデータ生成

### ガウスノイズを用いた現実的なデータシミュレーション
生成データに現実世界のゆらぎを持たせるため、Javaの `nextGaussian()` を用いて標準正規分布に従う乱数を生成しています。
- **スケーリング**: 標準偏差を 1.5倍（$\sigma=1.5$）に広げることで、意図的に「裾（テール）」を引き延ばし、割安物件や外れ値が発生しやすい分布を創出しました。
- **データバリデーション**: `Math.max(rent, 3)` によるガードレールを設置し、統計的な遊びとビジネス上の整合性を両立させています。

### Rによる有意性検定（p値の算出）
AIによる予測結果の信頼性を担保するため、`rpy2` を介してRの `stats::lm` 関数を呼び出しています。
- 築年数などの説明変数が家賃に与える影響の **p値** を取得し、`p < 0.05` の場合にのみ「統計的に有意である」と判定する厳密なエビデンスをユーザーに提供しています。

---

## 3. Web実装における最適化

### Django ORM の効率的な運用
- **遅延評価（Lazy Evaluation）の活用**: 検索フィルタリングにおいて、実際にデータが必要になるまでDB問い合わせを保留することで、クエリの発行回数を最適化しています。
- **Bulk Operations**: 大量データのDB登録には `bulk_create` を使用。100件のデータを1回のSQL発行で処理し、I/O負荷を大幅に削減しました。

### Fargate環境への最適化
- **Base64エンコーディングによる画像配信**: 分析グラフ（PNG）を一度バイナリデータとして読み込み、Base64形式の文字列に変換してHTMLへ直接埋め込んでいます。
- これにより、Fargateのようなエフェメラルな環境において、外部ストレージ（S3等）を介さずに高速かつ安定した画像表示を実現しました。

---

## 4. インフラ構築とトラブルシューティング

### AWS Fargate への移行とリソース管理
コスト予測可能性と運用の容易さを重視し、AWS Fargate (ECS) を採用しました。
- **メモリ不足の特定**: 当初 1GBで構成していましたが、R環境の初期化およびパッケージ読み込みによる「サイレント・フリーズ」が発生。ログ解析によりボトルネックを特定し、2GBへ増強することで安定稼働を実現しました。

### デバッグ記録：プロセスの競合
Djangoの開発用サーバー（`runserver`）が持つ自動リロード機能が Rのメインスレッド初期化と競合し、コンテナがフリーズする事象を確認。
- **解決策**: 開発環境では `--noreload` オプションで対応し、本番環境ではマルチプロセス管理に長けた **Gunicorn** を導入することで、プロセスの安定性を確保予定。

---

## 5. 今後の展望
- **Gunicorn プリロード設定の最適化**: `rpy2` の初期化タイミングを制御し、ワーカー起動の高速化を図る。
- **分析機能の拡張**: WebUIから任意の統計項目を選択できる動的な解析インターフェースの実装。
