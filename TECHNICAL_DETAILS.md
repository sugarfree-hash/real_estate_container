# Technical Deep Dive: 開発の詳細と意思決定プロセス

本ドキュメントでは、ポートフォリオ開発における技術的な選定理由、設計思想、および直面した課題の解決プロセスについて詳細に記述します。

---

## 1. システムアーキテクチャ
[cite_start]本システムは、Javaによるデータ生成、DjangoによるWeb制御、Rによる高度な統計解析を組み合わせた、異種言語統合型のパイプラインを採用しています [cite: 10, 11, 12, 71]。

- **Data Generation**: Java (GSON)
- **Web Framework**: Django (Python)
- **Statistical Analytics**: R (ggplot2, stats)
- [cite_start]**Infrastructure**: Docker / AWS Fargate [cite: 216]

---

## 2. 統計的アプローチとデータ生成

### ガウスノイズを用いた現実的なデータシミュレーション
[cite_start]生成データに現実世界のゆらぎを持たせるため、Javaの `nextGaussian()` を用いて標準正規分布に従う乱数を生成しています [cite: 156]。
- [cite_start]**スケーリング**: 標準偏差を 1.5倍（$\sigma=1.5$）に広げることで、意図的に「裾（テール）」を引き延ばし、割安物件や外れ値が発生しやすい分布を創出しました [cite: 171, 172, 175]。
- [cite_start]**データバリデーション**: `Math.max(rent, 3)` によるガードレールを設置し、統計的な遊びとビジネス上の整合性を両立させています [cite: 178]。

### Rによる有意性検定（p値の算出）
[cite_start]AIによる予測結果の信頼性を担保するため、`rpy2` を介してRの `stats::lm` 関数を呼び出しています [cite: 98, 107]。
- [cite_start]築年数などの説明変数が家賃に与える影響の **p値** を取得し、`p < 0.05` の場合にのみ「統計的に有意である」と判定する厳密なエビデンスをユーザーに提供しています [cite: 108, 109, 110]。

---

## 3. Web実装における最適化

### Django ORM の効率的な運用
- [cite_start]**遅延評価（Lazy Evaluation）の活用**: 検索フィルタリングにおいて、実際にデータが必要になるまでDB問い合わせを保留することで、クエリの発行回数を最適化しています 。
- [cite_start]**Bulk Operations**: 大量データのDB登録には `bulk_create` を使用。100件のデータを1回のSQL発行で処理し、I/O負荷を大幅に削減しました [cite: 83, 84, 85]。

### Fargate環境への最適化
- [cite_start]**Base64エンコーディングによる画像配信**: 分析グラフ（PNG）を一度バイナリデータとして読み込み、Base64形式の文字列に変換してHTMLへ直接埋め込んでいます [cite: 116, 117]。
- [cite_start]これにより、Fargateのようなエフェメラルな環境において、外部ストレージ（S3等）を介さずに高速かつ安定した画像表示を実現しました [cite: 118]。

---

## 4. インフラ構築とトラブルシューティング

### AWS Fargate への移行とリソース管理
[cite_start]コスト予測可能性と運用の容易さを重視し、AWS Fargate (ECS) を採用しました [cite: 216, 217]。
- [cite_start]**メモリ不足の特定**: 当初 1GBで構成していましたが、R環境の初期化およびパッケージ読み込みによる「サイレント・フリーズ」が発生。ログ解析によりボトルネックを特定し、2GBへ増強することで安定稼働を実現しました [cite: 225]。

### デバッグ記録：プロセスの競合
[cite_start]Djangoの開発用サーバー（`runserver`）が持つ自動リロード機能が Rのメインスレッド初期化と競合し、コンテナがフリーズする事象を確認 [cite: 225]。
- [cite_start]**解決策**: 開発環境では `--noreload` オプションで対応し、本番環境ではマルチプロセス管理に長けた **Gunicorn** を導入することで、プロセスの安定性を確保予定 [cite: 128, 234]。

---

## 5. 今後の展望
- [cite_start]**Gunicorn プリロード設定の最適化**: `rpy2` の初期化タイミングを制御し、ワーカー起動の高速化を図る [cite: 128]。
- [cite_start]**分析機能の拡張**: WebUIから任意の統計項目を選択できる動的な解析インターフェースの実装 [cite: 129]。